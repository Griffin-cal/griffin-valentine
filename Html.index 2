<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Joyce & Griffin 💖</title>

<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">

<style>
body{
    margin:0;
    height:100vh;
    display:flex;
    justify-content:center;
    align-items:center;
    background:radial-gradient(circle at top,#1a001f,#000);
    font-family:'Poppins',sans-serif;
    overflow:hidden;
    color:white;
    opacity:0;
    animation:fadeIn 2s ease forwards;
}

@keyframes fadeIn{
    to{opacity:1;}
}

.card{
    text-align:center;
    padding:60px;
    border-radius:30px;
    backdrop-filter:blur(20px);
    background:rgba(255,255,255,0.05);
    box-shadow:0 0 60px rgba(255,0,120,0.4);
    transition:0.5s ease;
}

h1{
    font-family:'Playfair Display',serif;
    font-size:48px;
    background:linear-gradient(45deg,#ffd700,#ff4da6);
    -webkit-background-clip:text;
    -webkit-text-fill-color:transparent;
    margin:0;
    animation:glow 3s infinite alternate;
}

@keyframes glow{
    from{filter:drop-shadow(0 0 10px #ff4da6);}
    to{filter:drop-shadow(0 0 25px #ffd700);}
}

.subtitle{
    font-size:16px;
    opacity:0.6;
    margin-top:10px;
    letter-spacing:2px;
}

.question{
    margin-top:40px;
    font-size:22px;
    opacity:0.9;
}

button{
    padding:15px 40px;
    font-size:18px;
    border:none;
    border-radius:50px;
    margin:20px;
    cursor:pointer;
    transition:all 0.3s ease;
}

#yesBtn{
    background:linear-gradient(45deg,#ff0066,#ff4da6);
    color:white;
    box-shadow:0 0 20px #ff4da6;
}

#yesBtn:hover{
    transform:scale(1.1);
    box-shadow:0 0 35px #ff80bf;
}

#noBtn{
    background:#444;
    color:white;
    position:absolute;
}

.hidden{display:none;}

.celebrate{
    font-size:30px;
    margin-top:30px;
    animation:pulse 1s infinite alternate;
}

@keyframes pulse{
    from{transform:scale(1);}
    to{transform:scale(1.1);}
}

canvas{
    position:fixed;
    top:0;
    left:0;
    pointer-events:none;
}
</style>
</head>

<body>

<canvas id="fireworks"></canvas>

<div class="card">
    <div style="opacity:0.6;font-size:14px;letter-spacing:2px;">
        Every love story starts with a question...
    </div>

    <h1>Joyce 👑</h1>
    <div class="subtitle">From Griffin 💌</div>

    <div class="question">Will you be my Valentine?</div>

    <button id="yesBtn" onclick="sayYes()">YES 💖</button>
    <button id="noBtn">No 🙈</button>

    <div id="response" class="hidden celebrate">
        Joyce said YES to Griffin 😭💍✨
    </div>
</div>

<script>
const noBtn=document.getElementById("noBtn");
const yesBtn=document.getElementById("yesBtn");
const response=document.getElementById("response");
const card=document.querySelector(".card");

let scale=1;

noBtn.addEventListener("mouseover",()=>{
    const x=Math.random()*(window.innerWidth-100);
    const y=Math.random()*(window.innerHeight-50);
    noBtn.style.left=x+"px";
    noBtn.style.top=y+"px";

    scale+=0.2;
    yesBtn.style.transform=`scale(${scale})`;
});

function sayYes(){
    yesBtn.classList.add("hidden");
    noBtn.classList.add("hidden");
    response.classList.remove("hidden");
    card.style.boxShadow="0 0 100px rgba(255,215,0,0.8)";
    launchFireworks();
}

/* Fireworks */
const canvas=document.getElementById("fireworks");
const ctx=canvas.getContext("2d");
canvas.width=window.innerWidth;
canvas.height=window.innerHeight;

let particles=[];

function launchFireworks(){
    setInterval(()=>{
        for(let i=0;i<120;i++){
            particles.push({
                x:Math.random()*canvas.width,
                y:Math.random()*canvas.height/2,
                radius:2,
                color:`hsl(${Math.random()*360},100%,60%)`,
                speedX:(Math.random()-0.5)*6,
                speedY:(Math.random()-0.5)*6,
                life:100
            });
        }
    },700);
}

function animate(){
    requestAnimationFrame(animate);
    ctx.fillStyle="rgba(0,0,0,0.2)";
    ctx.fillRect(0,0,canvas.width,canvas.height);

    particles.forEach((p,index)=>{
        ctx.beginPath();
        ctx.arc(p.x,p.y,p.radius,0,Math.PI*2);
        ctx.fillStyle=p.color;
        ctx.fill();
        p.x+=p.speedX;
        p.y+=p.speedY;
        p.life--;
        if(p.life<=0)particles.splice(index,1);
    });
}
animate();
</script>

</body>
</html>